server:
  port: 5000
spring:
  application:
    name: order-ws
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: sa
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true